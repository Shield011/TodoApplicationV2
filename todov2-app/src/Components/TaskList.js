import React from 'react'
import TodoList from './TodoList'

function TaskList({todos}) {
    return (
        <div>
            <ul>
                {/* {todos.map(todo) => (
                     <TodoList text={todotodo.text} id={todo.id}/>

                ))} */}

               

            </ul>
        </div>
    )
}

export default TaskList
